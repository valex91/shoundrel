#!/usr/bin/env bash

DEBUG='true'

currentDir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

source $currentDir/lib/tui
source $currentDir/lib/utils
source $currentDir/lib/cards
source $currentDir/lib/array
source $currentDir/lib/draw

ROWS=0
COLS=0

#setup system
gotoTab
clearScreen
moveCursor

getSize ROWS COLS

getTermSize() {
 getSize ROWS COLS
 local deckL=${#DECK[@]}
 local roomL=${#ROOM[@]}
 if (( ($deckL + $roomL) > 0 )); then
	drawLoop
 else
	menuDrawLoop
 fi
}

cleanUp() {
	returnTab
	showCursor
}

# TRAPS
trap 'getTermSize' WINCH
trap cleanUp exit 


hideCursor

echo "$ROWS $COLS"


# STATE
HP=20
ROOM=()
DECK=()
ELEMENT_X=0
POTION_USED_THIS_TURN=false
SKIPPED_PREV_ROOM=false
EQUIPPED_WEAPON=0
_ROOM_SIZE_=4
DRAWN_CARD_SIZE=7
PREV_MONSTER_SLAYED=0
TOASTS=()

# ICONS
SWORD=$'\u2694'

generateRoom() {
 local currentRoomSize=${#ROOM[@]}
 local turnSkip=${1:-false}

 if [[ $turnSkip == true ]]; then
	 local amountToDraw=$(min 4 ${#DECK[@]} )
	 DECK=("${DECK[@]}" "${ROOM[@]}")
	 splice DECK 0 $amountToDraw ROOM
 else
	 local diff=$((_ROOM_SIZE_ - currentRoomSize))
   local justDrawn=()
	 local amountToDraw=$(min ${#DECK[@]} $diff)
	 splice DECK 0 $amountToDraw justDrawn
	 ROOM=("${justDrawn[@]}" "${ROOM[@]}")
 fi
}


drawLoop() {
	clearScreen
	moveCursor
	local currentIndex=0
	local isSelected=false

	local terminalCenterX=$(($ROWS / 2))
	local terminalCenterY=$(( ($COLS / 2) - $DRAWN_CARD_SIZE ))

	# DECK
  drawDeck "${#DECK[@]}" $(($terminalCenterY - ($DRAWN_CARD_SIZE * 2))) $terminalCenterX

	# ROOM
	for card in "${ROOM[@]}"; do
		isSelected=$(( currentIndex == ELEMENT_X ))
		# term is not 0 indexed pt 2
		local columnToDraw=$(( ($currentIndex * $DRAWN_CARD_SIZE + 1) + $terminalCenterY ))
		interpolateCard "$card" "$isSelected" "$columnToDraw" "$terminalCenterX"
		printf '  '
		(( currentIndex++ ))
	done

	# Stats panel
	drawStats "$HP" "$EQUIPPED_WEAPON" "$PREV_MONSTER_SLAYED"

# Toast
	local _toastLength=${#TOASTS[@]}

	if (( $_toastLength > 0 )); then
		local longestToast=$(longestMember TOASTS)

		local ti=0
		for ((ti=0; ti < $_toastLength; ti++)) {
			writeAtPosition $(( $ti + 1)) $(( $COLS-$longestToast )) "${TOASTS[ti]}" true
		}
	fi	


}

nextTurn() {
	SKIPPED_PREV_ROOM=false
	POTION_USED_THIS_TURN=false
	TOASTS=()
	generateRoom
}

strategy() {
	local seed=$1
	local value=$2

	case $seed in
		1|2)
			if [[ $PREV_MONSTER_SLAYED != 0 ]] && (( value > PREV_MONSTER_SLAYED )); then
				# return rn Im not putting warnings up
   			(( HP -= $value ))
			else
				local monsterInteractionResult=$((value - EQUIPPED_WEAPON))
				local weaponConsumption=$((EQUIPPED_WEAPON - value))
				local safeMonsterInteractionResult=$(clampFloor $monsterInteractionResult 0)
				local safeWeaponConsumption=$(clampFloor $weaponConsumption 0)
				if (( $EQUIPPED_WEAPON > 0 )); then
					PREV_MONSTER_SLAYED=$value
				fi

				(( HP -= $safeMonsterInteractionResult ))
			fi;;
		3) local hpWithPotion=$(( HP + value ))
			if [[ $POTION_USED_THIS_TURN == false ]]; then
				HP=$(max $hpWithPotion 20)
				POTION_USED_THIS_TURN=true
			else
				TOASTS=("${TOASTS[@]}" "Potion discarded — already healed this turn")
			fi;;
		4) EQUIPPED_WEAPON=$value
       PREV_MONSTER_SLAYED=0;;
	esac

	if (( $HP <= 0 )); then
		echo "lost"
		exit 0
	fi


}

gameLoopTick() {
	local pick=${ROOM[ELEMENT_X]}
	local value
	local seed

	# pick the selected card from the room and consume it
	local pickedIdx=$(findIndex ROOM $pick)
	splice ROOM $pickedIdx 1

	# parsing the picked card
	IFS='_' read -r value seed <<< "$pick"

	strategy "$seed" "$value"

	if (( ${#ROOM[@]} == 0 && ${#DECK[@]} == 0 )); then
		echo "win"
		exit 0
	fi

	if (( ${#ROOM[@]} <= 1 )); then
		nextTurn
	fi
}

skipRoom() {
 if [[ $SKIPPED_PREV_ROOM == false ]]; then
	 generateRoom true
	 SKIPPED_PREV_ROOM=true
 else
	TOASTS=("${TOASTS[@]}" "Cannot skip two rooms in a row")
 fi
}

handleGameKeys() {
 local input=$1

 case $input in
	 a) (( ELEMENT_X = (ELEMENT_X - 1 + ${#ROOM[@]} ) % ${#ROOM[@]}  ));;
	 d) (( ELEMENT_X = (ELEMENT_X + 1) % ${#ROOM[@]}  ));;
	 e) gameLoopTick $input;;
	 n) skipRoom;;
	esac
}

playGame() {
	# INIT
	createDeck DECK
	generateRoom
	drawLoop

	 # Main loop reading keys
	while true; do
		read -rsn1 key
		mainGameLoop $key
	done
}

handleMenuKeys() {
	local input=$1
	case $input in
		p)  
			playGame;;

	esac
}

menuDrawLoop() {
 clearScreen
 moveCursor

 local titleL=$(getTitleAsciiLength)
 drawTitle 1 "$(( (COLS / 2) - ( titleL /2) ))"
}

mainGameLoop() {
 local inputK=$1
 handleGameKeys "$inputK"
 drawLoop
}

mainMenuLoop() {
 local inputK=$1
 menuDrawLoop
 handleMenuKeys "$inputK"
}



# Main loop
menuDrawLoop
while true; do
	read -rsn1  key
	mainMenuLoop $key
done


